services:
  - type: web
    name: socal-surfline-mcp
    runtime: python
    plan: free
    rootDir: mcp_server
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
